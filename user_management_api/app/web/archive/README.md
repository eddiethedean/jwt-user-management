## Archived legacy HTML UI

This folder contains the legacy HTML/CSS/JS UI that used to ship with the FastAPI app.

It is **not mounted or served** by the API anymore; the supported UI is `streamlit_user/`.

Contents were moved here to keep historical reference while ensuring the backend
remains an API-only service (better compatibility with Posit Connect cookie policies).

