DATABASE_URL=sqlite:///./app.db
JWT_SECRET=dev-secret
JWT_ALGORITHM=HS256
JWT_EXPIRES_MINUTES=60

# Optional: seed an initial admin user during `alembic upgrade head`.
# Defaults are used if these are unset.
SEED_ADMIN_EMAIL=
SEED_ADMIN_PASSWORD=
