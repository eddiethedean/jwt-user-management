# Base URL of the FastAPI API (no trailing slash). Required for most deployments.
BACKEND_URL=http://127.0.0.1:8001

# Optional: set DEBUG=1 for sidebar diagnostics.
# If BACKEND_URL is unset, the UI builds http://localhost:{PORT}{BASE_PATH}; defaults
# for PORT and BASE_PATH live in `config.py` in this directory.
