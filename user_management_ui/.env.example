# Required in most deployments: full base URL of the FastAPI API (no trailing slash).
BACKEND_URL=http://127.0.0.1:8001

# If BACKEND_URL is unset, local dev falls back to http://localhost:${PORT}${BASE_PATH}.
# PORT=8001
# BASE_PATH=

# DEBUG enables the in-app sidebar debug panel.
DEBUG=false
