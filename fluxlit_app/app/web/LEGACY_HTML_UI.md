# Legacy HTML UI

The interactive browser UI was previously implemented as server-rendered HTML under `app/web/`.

That UI is **retired**. The supported experience is the **FluxLit** app in this repository (FastAPI JSON API plus Streamlit).

If you still need HTML-era behavior for forensic or migration work, consult the templates under `app/web/archive/`.
